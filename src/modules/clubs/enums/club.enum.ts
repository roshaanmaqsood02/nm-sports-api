export enum ClubStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ARCHIVED = 'archived',
}

export enum ClubGender {
  MALE = 'male',
  FEMALE = 'female',
  MIXED = 'mixed',
  NON_BINARY = 'non_binary',
}
