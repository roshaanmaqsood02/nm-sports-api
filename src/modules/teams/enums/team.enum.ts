export enum TeamStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ARCHIVED = 'archived',
}

export enum TeamGender {
  MALE = 'male',
  FEMALE = 'female',
  MIXED = 'mixed',
  NON_BINARY = 'non_binary',
}

export enum TeamType {
  CLUB = 'club',
  LEAGUE = 'league',
}
